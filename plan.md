rules
Must be vanila js
No server
no ES6 modules
Must work on mobile and desktop, click and touch.
Organize into classes with separate files.
Keep code and logic small, concise, interpretable

phase 1
Make an App class in its own file

properties

stringIntervals = default = [4,4,3,4]
= degree steps to the next string

fretboardCanvas
Displays scale degrees in their x = fret, y = string location with a circle around each one.

give it these funtions
semitonesToDegree() in digits 1,b2,2,b3,3 etc
degreeToSemitones()
calcSemitones(stringIndex,fretIndex)
updateCanvas()

